name: Supabase Keep Alive

on:
  schedule:
    # Runs every 6 hours
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase Edge Function
        run: |
          curl -X GET https://cyimacxbdjaagqywoujn.supabase.co/functions/v1/keep-alive
